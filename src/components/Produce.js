import React from 'react';

const produceColumn = {
  display: 'inline-block',
  width: '80%'
};

const Produce = () => {
  return (
    <div style={produceColumn}>
      Produce
    </div>
  );
}

export default Produce;
